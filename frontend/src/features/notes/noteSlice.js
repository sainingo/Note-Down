import { createSlice, createAsyncThunk} from '@reduxjs/toolkit'

export const createNotes = createAsyncThunk('api/ideas', async (idea, {rejectWithValue}) => {
    try {
        
    } catch (error) {
        
    }

})